#1 -- 变量值的替换
	语法：  $(var:a=b) 或 ${var:a=b}
	注意：替换表达式中不能有空格！！

======================

#2 -- 变量的模式替换
	使用%保留变量值中的指定字符，替换其他字符

======================

#3 -- override关键字，用于指示makefile中定义的变量不能被覆盖
		define关键字，用于在makefile中定义多行变量；从变量名开始，endef结束

======================

