#1 -- 如果在makefile中定义了clean的同时、命令目录下存在同名的clean文件，
那么make命令会优先处理clean文件，而不是makefile中的clean命令

======================

#2 -- 伪目标的声明通过.PHONY关键字
伪目标不对应实际文件；且不管其依赖是否更新，命令总是执行

======================

#3 -- 绕开.PHONY关键字定义伪目标
如果一个规则没有命令或依赖，且它的目标不是一个存在的文件名
在执行此规则时，目标总会被认为是最新的

======================

