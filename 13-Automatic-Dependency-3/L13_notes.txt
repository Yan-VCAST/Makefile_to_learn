#1 -- include试用减号(-)不但关闭了include发出的警告，也同时关闭了错误
		当错误发生是，make将忽略这些错误

======================

#2 -- include一个文件之后，make会检查它对应的规则有没有，如果有，会进一步查看依赖和目标的时间戳的新旧

======================

#3 -- include的目标文件不存在，则以文件名查找规则，并执行
		include的目标文件不存在，且查找到的规则中创建了目标文件，则将创建成功的目标文件包含进当前makefile

======================

